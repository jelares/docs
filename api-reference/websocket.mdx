---
title: "New file"
description: "# Realtime / Streaming Some operations (e.g. TRMG reflection streaming, agent responses) use **WebSockets**. **Endpoint** - `wss://api.yourdomain.com/realtime` - Protocol: JSON messages - Auth: same bearer token as REST "
---